---
import PortfolioLayout from '@/layouts/PortfolioLayout.astro'
import CvPageContent from '@/components/portfolio/CvPageContent.astro'
import { themeConfig } from '@/config'
import '@/styles/cv-print.css'
---

<PortfolioLayout
  title={`CV — ${themeConfig.site.title}`}
  description={`Resume for ${themeConfig.site.author}`}
  mainClass="py-section-gap-lg flex flex-col gap-section-gap-lg w-full"
>
  <!-- <CvPageContent /> -->
</PortfolioLayout>
